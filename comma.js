// Q4. Describe the usage of the comma operator in JavaScript and provide an example.
  const a=1,b=3,c=6;
  /* Comma operator allows us to evaluate multiple expressions in a single statement
  like declaring multiple
variables in a single statement separating them with the comma operator. */
   console.log(a,b,c);// you can prrint like this: